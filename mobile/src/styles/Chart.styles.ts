import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 6,
    paddingVertical: 16,
    backgroundColor: '#fff',
    marginVertical: 8,
  },
  chart: {
    borderRadius: 8,
    marginVertical: 8,
  },
});

export default styles;